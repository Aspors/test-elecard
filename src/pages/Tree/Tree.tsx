import React from "react";
import { TreeList } from "../../components";

const Tree = () => {
  return (
    <section className={"tree"}>
      <div className="container">
        <TreeList />
      </div>
    </section>
  );
};

export default Tree;
